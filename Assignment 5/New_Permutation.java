package Assignment_5;

public class New_Permutation {
    /*
    #include<bits/stdc++.h>
using namespace std;

void foo(int q,string strr,string tee)
{
    if(q==strr.size())
    {
        cout<<tee<<" ";
        return;
    }
    if(strr[q]-'0'>=0 && strr[q]-'0'<=9)
    {
        foo(q+1,strr,tee+strr[q]);
        return;
    }
    char upp =tolower(strr[q]);
    foo(q+1,strr,tee+upp);
    upp= toupper(strr[q]);
    foo(q+1,strr,tee+upp);
}
int main(){

    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    string strr;cin>>strr;
    foo(0,strr,"");
    return 0;
}
     */
}
